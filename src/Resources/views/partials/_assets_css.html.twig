{#
    The VideoOptimizer scoped stylesheet (everything lives under `.vo-*`). Single source of the CSS
    asset path — the AssetInjectionListener renders this into <head>, and assets.html.twig includes
    it for the manual/opt-out path.
#}
<link rel="stylesheet" href="{{ asset('bundles/scalevideooptimizer/css/vo-blocks.css') }}">
